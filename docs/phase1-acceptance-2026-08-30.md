# Phase 1 packaged acceptance record — 2026-08-30

This record separates evidence collected from evidence still required. A successful parser test,
build, or process launch is not counted as visual acceptance for a renderer family.

## Build and automated checks

- Release `wxlens-app` build: **pass**. QML AOT compilation completed for the application,
  including `TopBar.qml`, `PaneHost.qml`, `ProductBrowser.qml`, `Level3ProductLayer.qml`, and
  `WeatherOverlaysLayer.qml`. The deployed executable was regenerated by the normal
  `windeployqt` post-build step.
- Working-tree QML validation: **pass**. The existing `TopBar.qml` and `PaneHost.qml` edits compile
  in Release; `git diff --check` reports no whitespace errors.
- WxLens model tests: **126/126 pass** (CTest tests 202-327). This includes Level 3 radial, raster,
  graphic-overlay, graphic/tabular text, product identity, palette synchronization and selected-
  storm synchronization coverage.
- Full CTest discovery: **324/327 pass**, with 12 additional tests skipped by their own runtime
  prerequisites. The three non-passing entries are not new WxLens model failures:
  - `test_mln_quick` was not built and therefore was not run.
  - `IemApiProviderTest.ListTextProducts` and `IemApiProviderTest.LoadTextProducts` fail in the
    read-only legacy wxdata suite against the current live IEM endpoint.

## Packaged visual checks

| Scenario | Evidence | Result |
|---|---|---|
| 1x1 live Level 2 reflectivity, KEAX | Maximized packaged Release app at 2048x1280 | **Pass** — radar and basemap rendered; process remained responsive |
| Level 2 velocity | No captured product switch | **Open** |
| Level 3 radial | Automated renderer-contract fixtures only | **Open for packaged visual acceptance** |
| Level 3 BA07/BA0F raster | Automated NCR fixture only | **Open for packaged visual acceptance** |
| Level 3 storm graphic overlay and hit-selection | Automated NST translation/synchronization tests only | **Open for packaged visual acceptance** |
| Level 3 graphic/tabular text details | Automated NST/SPD contract tests only | **Open for packaged visual acceptance** |
| 2x2 and 3x3 mixed-family synchronized/independent panes | No packaged run captured | **Open** |

The initial live screenshot and raw one-second process samples are local build artifacts under
`build-release-vs2026/`; they are intentionally not source-controlled release evidence.

## Preliminary performance evidence

The 60-second 1x1 process sample is recorded in `docs/performance-baseline.md`. It covers CPU and
memory only. Frame time/FPS, GPU utilization, decode latency, request/cache behavior, 2x2/3x3
layouts and repeated live updates remain open.

## Next acceptance action

Run the packaged application interactively and capture one dated artifact for every open row in
the table, using exact site/AWIPS ID/time selections. For each 1x1, 2x2 and 3x3 run, collect the
full counter set required by `docs/performance-baseline.md`. Only then may the Level 3 completion
rule and performance gate be checked off in `docs/ROADMAP.md`.
